public class SavingsAccount extends AccountInfo {

    public SavingsAccount(String firstName, String lastName, String emailAddress, long accountNo) {
        super(firstName, lastName, emailAddress, accountNo);
    }
}
